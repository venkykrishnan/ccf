package ccf.domain.tabular.Dimension;

public enum DimensionType {
    STRING, // for text values. for example, account name
    BOOLEAN, // for true/false values. for example, is active
    DATE, // for date values. for example, job start date
    MONTH, // for month values. for example, posting period
}